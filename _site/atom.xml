<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

 <title>proxpero</title>
 <link href="http://proxpero.com/atom.xml" rel="self"/>
 <link href="http://proxpero.com/"/>
 <updated>2016-12-19T22:31:00-05:00</updated>
 <id>http://proxpero.com</id>
 <author>
   <name>Todd Olsen</name>
   <email>proxpero@gmail.com</email>
 </author>

 
 <entry>
   <title>Advent of Code 2016</title>
   <link href="http://proxpero.com/blog/2016/12/19/Advent-of-Code-2016"/>
   <updated>2016-12-19T00:00:00-05:00</updated>
   <id>http://proxpero.com/blog/2016/12/19/Advent-of-Code-2016</id>
   <content type="html">&lt;h2 id=&quot;the-problem&quot;&gt;The Problem&lt;/h2&gt;

&lt;p&gt;I had a view with a simple vertical stack of subviews and I wanted to give the user the ability to reorder the subviews using drag-and-drop. Instead of implementing AppKit’s drag-and-drop API, I wrote my own mechanism by hooking into &lt;code&gt;NSResponder&lt;/code&gt;’s &lt;code&gt;mouseDragged(...)&lt;/code&gt; method and &lt;code&gt;NSWindow&lt;/code&gt;’s &lt;code&gt;trackEventsMatchingMask(...) -&amp;gt; Void&lt;/code&gt;. That way I could control the logic at the relevant steps during the user’s action, without relying on AppKit’s unseen magic. Finally, I refactored the logic into protocol extensions.&lt;/p&gt;

&lt;h2 id=&quot;the-setup&quot;&gt;The Setup&lt;/h2&gt;

&lt;p&gt;I start with a view that contains the subviews I need to reorder. Following Cocoa’s drag-and-drop API, I call this container view the Target. There are two important methods to implement. The first is &lt;code&gt;layoutSubviews(subviews: [NSView])&lt;/code&gt;.&lt;/p&gt;

&lt;figure class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-swift&quot; data-lang=&quot;swift&quot;&gt;func layoutSubviews(subviews: [NSView]) {
        
        removeConstraints(constraints)
        if subviews.isEmpty { return }
        
        var prev: NSView?
        
        for subview in subviews {
            
            addConstraints(
            	NSLayoutConstraint.constraintsWithVisualFormat(&amp;quot;H:|[subview]|&amp;quot;,
					options: [], 
            		metrics: nil,
            		views: [&amp;quot;subview&amp;quot;: subview]
				)
			)
            addConstraint(
                NSLayoutConstraint(
                    item:       subview,
                    attribute:  .Top,
                    relatedBy:  .Equal,
                    toItem:     prev != nil ? prev!     : self,
                    attribute:  prev != nil ? .Bottom   : .Top,
                    multiplier: 1,
                    constant:   prev != nil ? 1         : 0)
            )
            prev = subview
        }
        
        addConstraint(NSLayoutConstraint(
        	item: prev!, 
        	attribute: .Bottom, 
        	relatedBy: .Equal, 
        	toItem: self, 
        	attribute: .Bottom, 
        	multiplier: 1, 
        	constant: 0)
		)
        
    }&lt;/code&gt;&lt;/pre&gt;&lt;/figure&gt;

&lt;p&gt;You pass into this method an array of subviews and it will use autolayout to arrange the subviews from top to botton to match the order of the array. It assumes these subviews have already been added to the view: the superview never adds or removes any of these subviews during the drag operation.&lt;/p&gt;

&lt;h2 id=&quot;the-method&quot;&gt;The Method&lt;/h2&gt;

&lt;p&gt;Before looking at the second method the Target view is required to implement, I’ll explain the basic theory behind the solution. When the user tries to drag a view (I call it the “Source View”, following Cocoa’s drag and drop documentation), the &lt;code&gt;mouseDragged(theEvent: NSEvent)&lt;/code&gt; method is called on the view (because it inherits from &lt;code&gt;NSResponder&lt;/code&gt;). My Source view subclass overrides this method in order to tell its superview that a drag is beginning. It does this by calling that second method on the superview I referred to earlier.&lt;/p&gt;

&lt;figure class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-swift&quot; data-lang=&quot;swift&quot;&gt;// In the subview, an NSView subclass

    override func mouseDragged(theEvent: NSEvent) {

        if let target = superview as? MyTargetView {
            target.reorderSubview(self, withEvent: theEvent)
        }
        
    }&lt;/code&gt;&lt;/pre&gt;&lt;/figure&gt;

&lt;p&gt;The superview, what I’m calling the Target, now knows to do several things. Here’s the full method, then I’ll explain the steps.&lt;/p&gt;

&lt;figure class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-swift&quot; data-lang=&quot;swift&quot;&gt;// In the superview, an NSView subclass

    func reorderSubview(subview: Source, withEvent event: NSEvent) {
        
        let position        = NSMaxY(frame) - NSMaxY(subview.frame)
        let initial         = convertPoint(event.locationInWindow, fromView: nil).y
        
        let draggingView    = subview.draggingView
        
        let draggingConstraints = [
            NSLayoutConstraint(item: draggingView, attribute: .Top,      relatedBy: .Equal, toItem: self, attribute: .Top,       multiplier: 1, constant: position),
            NSLayoutConstraint(item: draggingView, attribute: .Leading,  relatedBy: .Equal, toItem: self, attribute: .Leading,   multiplier: 1, constant: 0),
            NSLayoutConstraint(item: draggingView, attribute: .Trailing, relatedBy: .Equal, toItem: self, attribute: .Trailing,  multiplier: 1, constant: 0)
        ]
        
        addSubview(draggingView)
        addConstraints(draggingConstraints)
        subview.hidden = true
        
        var previous = initial
        
        window?.trackEventsMatchingMask([.LeftMouseUpMask, .LeftMouseDraggedMask], timeout: NSDate.distantFuture().timeIntervalSinceNow, mode: NSEventTrackingRunLoopMode) { [unowned self] (dragEvent, stop) in
            
            guard dragEvent.type != NSEventType.LeftMouseUp else {
                
                draggingView.removeFromSuperview()
                self.removeConstraints(draggingConstraints)
                subview.hidden = false
                stop.memory = true
                return
                
            }
            
            let next = self.convertPoint(dragEvent.locationInWindow, fromView: nil).y
            
            draggingConstraints[0].constant = position + initial - next
        
            let middle      = NSMidY(draggingView.frame)
            let top         = NSMaxY(subview.frame)
            let bottom      = NSMinY(subview.frame)
            
            let movingUp    = next &amp;gt; previous &amp;amp;&amp;amp; (middle &amp;gt; top)
            let movingDown  = next &amp;lt; previous &amp;amp;&amp;amp; (middle &amp;lt; bottom)
            
            func moveSubview(direction: DragDirection) {
                
                let index = self.subviews.indexOf(subview)!
                let removeIndex: Int
                let insertIndex: Int
                
                switch direction {
                    case .Up:
                        removeIndex = index
                        insertIndex = index - 1
                    case .Down:
                        removeIndex = index + 1
                        insertIndex = index
                }
                
                let temp = self.subviews.removeAtIndex(removeIndex)
                self.subviews.insert(temp, atIndex: insertIndex)
                self.layoutSubviews(self.subviews.filter { $0 != draggingView })
                self.addConstraints(draggingConstraints)
                
            }
            
            if movingUp &amp;amp;&amp;amp; subview != self.subviews.first! {
                moveSubview(.Up)
            }
            
            if movingDown &amp;amp;&amp;amp; subview != self.subviews.last! {
                moveSubview(.Down)
            }
            
            previous = next
            
        }&lt;/code&gt;&lt;/pre&gt;&lt;/figure&gt;

&lt;ol&gt;
  &lt;li&gt;
    &lt;p&gt;First, the methods extracts the vertical distance from the top of the superview to the top of the dragged subview and saves it in &lt;code&gt;position&lt;/code&gt; and it gets the &lt;code&gt;y&lt;/code&gt; coordinate of the mouse-down location from &lt;code&gt;theEvent&lt;/code&gt; and saves it in &lt;code&gt;initial&lt;/code&gt;.&lt;/p&gt;
  &lt;/li&gt;
  &lt;li&gt;
    &lt;p&gt;Then, it gets an image of the subview being dragged and adds it to the superview. &lt;code&gt;draggingConstants&lt;/code&gt; are set up so that the copy is superimposed directly over the original subview. This copy will &lt;em&gt;appear&lt;/em&gt; to the user as the original subview being dragged. But in fact, that subview has its &lt;code&gt;hidden&lt;/code&gt; property set to &lt;code&gt;true&lt;/code&gt; during the drag operation. Hiding the subview allows it to still provide the user feedback as a placeholder of the subview’s new position.&lt;/p&gt;
  &lt;/li&gt;
  &lt;li&gt;
    &lt;p&gt;The method calls the window’s &lt;code&gt;trackEventsMatchingMask&lt;/code&gt; method, and supplies it the block of code that makes up the remainder of the method. The user either drags the mouse or releases the button.&lt;/p&gt;

    &lt;ul&gt;
      &lt;li&gt;
        &lt;p&gt;If the user releases the mouse, the &lt;code&gt;NSEventType.LeftMouseUp&lt;/code&gt; tells the block to stop and the window stops tracking mouse events for the method. The copy of the subview is removed and the subview becomes unhidden.&lt;/p&gt;
      &lt;/li&gt;
      &lt;li&gt;
        &lt;p&gt;Otherwise, as the user continues to drag the mouse, the vertical distance between the current mouse position and the previous one is subtracted from the dragging view’s layout constraint constant anchoring it to the top of the superview. So the dragging view moves up and down as the mouse goes up and down.&lt;/p&gt;
      &lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;
    &lt;p&gt;If the mouse drags far enough that it needs to switch position with another view, the nested function &lt;code&gt;moveSubview(direction: DragDirection)&lt;/code&gt; is called and the subviews are first reordered and then &lt;code&gt;layoutSubviews&lt;/code&gt; is called, updating the UI to match the new order.&lt;/p&gt;
  &lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;Finally, let me say that much of this technique was inspired by &lt;a href=&quot;https://github.com/monyschuk/LITabControl&quot;&gt;this project&lt;/a&gt; (Objc) by Mark Onyschuk, in which he builds a tab view control much like the one seen in Numbers.app. Thanks Mark!&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>A Simple Drag-and-Drop View in Swift</title>
   <link href="http://proxpero.com/blog/2016/01/18/Drag-and-Drop-View"/>
   <updated>2016-01-18T00:00:00-05:00</updated>
   <id>http://proxpero.com/blog/2016/01/18/Drag-and-Drop-View</id>
   <content type="html">&lt;h2 id=&quot;the-problem&quot;&gt;The Problem&lt;/h2&gt;

&lt;p&gt;I had a view with a simple vertical stack of subviews and I wanted to give the user the ability to reorder the subviews using drag-and-drop. Instead of implementing AppKit’s drag-and-drop API, I wrote my own mechanism by hooking into &lt;code&gt;NSResponder&lt;/code&gt;’s &lt;code&gt;mouseDragged(...)&lt;/code&gt; method and &lt;code&gt;NSWindow&lt;/code&gt;’s &lt;code&gt;trackEventsMatchingMask(...) -&amp;gt; Void&lt;/code&gt;. That way I could control the logic at the relevant steps during the user’s action, without relying on AppKit’s unseen magic. Finally, I refactored the logic into protocol extensions.&lt;/p&gt;

&lt;h2 id=&quot;the-setup&quot;&gt;The Setup&lt;/h2&gt;

&lt;p&gt;I start with a view that contains the subviews I need to reorder. Following Cocoa’s drag-and-drop API, I call this container view the Target. There are two important methods to implement. The first is &lt;code&gt;layoutSubviews(subviews: [NSView])&lt;/code&gt;.&lt;/p&gt;

&lt;figure class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-swift&quot; data-lang=&quot;swift&quot;&gt;func layoutSubviews(subviews: [NSView]) {

        removeConstraints(constraints)
        if subviews.isEmpty { return }

        var prev: NSView?

        for subview in subviews {

            addConstraints(
            	NSLayoutConstraint.constraintsWithVisualFormat(&amp;quot;H:|[subview]|&amp;quot;,
					options: [],
            		metrics: nil,
            		views: [&amp;quot;subview&amp;quot;: subview]
				)
			)
            addConstraint(
                NSLayoutConstraint(
                    item:       subview,
                    attribute:  .Top,
                    relatedBy:  .Equal,
                    toItem:     prev != nil ? prev!     : self,
                    attribute:  prev != nil ? .Bottom   : .Top,
                    multiplier: 1,
                    constant:   prev != nil ? 1         : 0)
            )
            prev = subview
        }

        addConstraint(NSLayoutConstraint(
        	item: prev!,
        	attribute: .Bottom,
        	relatedBy: .Equal,
        	toItem: self,
        	attribute: .Bottom,
        	multiplier: 1,
        	constant: 0)
		)

    }&lt;/code&gt;&lt;/pre&gt;&lt;/figure&gt;

&lt;p&gt;You pass into this method an array of subviews and it will use autolayout to arrange the subviews from top to botton to match the order of the array. It assumes these subviews have already been added to the view: the superview never adds or removes any of these subviews during the drag operation.&lt;/p&gt;

&lt;h2 id=&quot;the-method&quot;&gt;The Method&lt;/h2&gt;

&lt;p&gt;Before looking at the second method the Target view is required to implement, I’ll explain the basic theory behind the solution. When the user tries to drag a view (I call it the “Source View”, following Cocoa’s drag and drop documentation), the &lt;code&gt;mouseDragged(theEvent: NSEvent)&lt;/code&gt; method is called on the view (because it inherits from &lt;code&gt;NSResponder&lt;/code&gt;). My Source view subclass overrides this method in order to tell its superview that a drag is beginning. It does this by calling that second method on the superview I referred to earlier.&lt;/p&gt;

&lt;figure class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-swift&quot; data-lang=&quot;swift&quot;&gt;// In the subview, an NSView subclass

    override func mouseDragged(theEvent: NSEvent) {

        if let target = superview as? MyTargetView {
            target.reorderSubview(self, withEvent: theEvent)
        }

    }&lt;/code&gt;&lt;/pre&gt;&lt;/figure&gt;

&lt;p&gt;The superview, what I’m calling the Target, now knows to do several things. Here’s the full method, then I’ll explain the steps.&lt;/p&gt;

&lt;figure class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-ruby&quot; data-lang=&quot;ruby&quot;&gt;// In the superview, an NSView subclass

    func reorderSubview(subview: Source, withEvent event: NSEvent) {

        let position        = NSMaxY(frame) - NSMaxY(subview.frame)
        let initial         = convertPoint(event.locationInWindow, fromView: nil).y

        let draggingView    = subview.draggingView

        let draggingConstraints = [
            NSLayoutConstraint(item: draggingView, attribute: .Top,      relatedBy: .Equal, toItem: self, attribute: .Top,       multiplier: 1, constant: position),
            NSLayoutConstraint(item: draggingView, attribute: .Leading,  relatedBy: .Equal, toItem: self, attribute: .Leading,   multiplier: 1, constant: 0),
            NSLayoutConstraint(item: draggingView, attribute: .Trailing, relatedBy: .Equal, toItem: self, attribute: .Trailing,  multiplier: 1, constant: 0)
        ]

        addSubview(draggingView)
        addConstraints(draggingConstraints)
        subview.hidden = true

        var previous = initial

        window?.trackEventsMatchingMask([.LeftMouseUpMask, .LeftMouseDraggedMask], timeout: NSDate.distantFuture().timeIntervalSinceNow, mode: NSEventTrackingRunLoopMode) { [unowned self] (dragEvent, stop) in

            guard dragEvent.type != NSEventType.LeftMouseUp else {

                draggingView.removeFromSuperview()
                self.removeConstraints(draggingConstraints)
                subview.hidden = false
                stop.memory = true
                return

            }

            let next = self.convertPoint(dragEvent.locationInWindow, fromView: nil).y

            draggingConstraints[0].constant = position + initial - next

            let middle      = NSMidY(draggingView.frame)
            let top         = NSMaxY(subview.frame)
            let bottom      = NSMinY(subview.frame)

            let movingUp    = next &amp;gt; previous &amp;amp;&amp;amp; (middle &amp;gt; top)
            let movingDown  = next &amp;lt; previous &amp;amp;&amp;amp; (middle &amp;lt; bottom)

            func moveSubview(direction: DragDirection) {

                let index = self.subviews.indexOf(subview)!
                let removeIndex: Int
                let insertIndex: Int

                switch direction {
                    case .Up:
                        removeIndex = index
                        insertIndex = index - 1
                    case .Down:
                        removeIndex = index + 1
                        insertIndex = index
                }

                let temp = self.subviews.removeAtIndex(removeIndex)
                self.subviews.insert(temp, atIndex: insertIndex)
                self.layoutSubviews(self.subviews.filter { $0 != draggingView })
                self.addConstraints(draggingConstraints)

            }

            if movingUp &amp;amp;&amp;amp; subview != self.subviews.first! {
                moveSubview(.Up)
            }

            if movingDown &amp;amp;&amp;amp; subview != self.subviews.last! {
                moveSubview(.Down)
            }

            previous = next

        }&lt;/code&gt;&lt;/pre&gt;&lt;/figure&gt;

&lt;ol&gt;
  &lt;li&gt;
    &lt;p&gt;First, the methods extracts the vertical distance from the top of the superview to the top of the dragged subview and saves it in &lt;code&gt;position&lt;/code&gt; and it gets the &lt;code&gt;y&lt;/code&gt; coordinate of the mouse-down location from &lt;code&gt;theEvent&lt;/code&gt; and saves it in &lt;code&gt;initial&lt;/code&gt;.&lt;/p&gt;
  &lt;/li&gt;
  &lt;li&gt;
    &lt;p&gt;Then, it gets an image of the subview being dragged and adds it to the superview. &lt;code&gt;draggingConstants&lt;/code&gt; are set up so that the copy is superimposed directly over the original subview. This copy will &lt;em&gt;appear&lt;/em&gt; to the user as the original subview being dragged. But in fact, that subview has its &lt;code&gt;hidden&lt;/code&gt; property set to &lt;code&gt;true&lt;/code&gt; during the drag operation. Hiding the subview allows it to still provide the user feedback as a placeholder of the subview’s new position.&lt;/p&gt;
  &lt;/li&gt;
  &lt;li&gt;
    &lt;p&gt;The method calls the window’s &lt;code&gt;trackEventsMatchingMask&lt;/code&gt; method, and supplies it the block of code that makes up the remainder of the method. The user either drags the mouse or releases the button.&lt;/p&gt;

    &lt;ul&gt;
      &lt;li&gt;
        &lt;p&gt;If the user releases the mouse, the &lt;code&gt;NSEventType.LeftMouseUp&lt;/code&gt; tells the block to stop and the window stops tracking mouse events for the method. The copy of the subview is removed and the subview becomes unhidden.&lt;/p&gt;
      &lt;/li&gt;
      &lt;li&gt;
        &lt;p&gt;Otherwise, as the user continues to drag the mouse, the vertical distance between the current mouse position and the previous one is subtracted from the dragging view’s layout constraint constant anchoring it to the top of the superview. So the dragging view moves up and down as the mouse goes up and down.&lt;/p&gt;
      &lt;/li&gt;
    &lt;/ul&gt;
  &lt;/li&gt;
  &lt;li&gt;
    &lt;p&gt;If the mouse drags far enough that it needs to switch position with another view, the nested function &lt;code&gt;moveSubview(direction: DragDirection)&lt;/code&gt; is called and the subviews are first reordered and then &lt;code&gt;layoutSubviews&lt;/code&gt; is called, updating the UI to match the new order.&lt;/p&gt;
  &lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;Finally, let me say that much of this technique was inspired by &lt;a href=&quot;https://github.com/monyschuk/LITabControl&quot;&gt;this project&lt;/a&gt; (Objc) by Mark Onyschuk, in which he builds a tab view control much like the one seen in Numbers.app. Thanks Mark!&lt;/p&gt;
</content>
 </entry>
 

</feed>
